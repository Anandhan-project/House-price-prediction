# House-price-prediction
house price prediction with basic inbuild interface
used basic model - linear regression,random forest regression,svm
the interface is python inbuild tkinter (interface is incompleted
